# Changelog

## [Unreleased]

### Added

- Deepened Python, Go, and Rust adapters with scoped declarations and conservative source-backed call, inheritance, receiver, and explicit implementation relationships, reported under an honest hybrid precision tier.
- Added trusted `.pi/architecture.json` fitness evaluation for dependency prohibitions, allowed boundaries, cycles, fan-in budgets, and stable violation baselines.
- Registered the active logical-workspace graph as the provider for Goal mode's default revision-locked impact completion gate.
- Added conservative Python, Go, and Rust structural adapters with stable declarations, workspace import resolution, adapter status, and cross-language-safe impact indexing.
- Added PatchSet-aware verification planning and execution through explicit `.pi/checks.json` catalogs, transitive CodeGraph impact, fail-closed coverage, and content-addressed evidence bundles.
- Added workspace-view-aware indexing so CodeGraph follows transactional overlay roots and fails closed for inaccessible execution-boundary paths.
- Added an optional on-demand TypeScript and JavaScript code graph with persistent incremental indexing and bounded symbol, dependency, dependent, and impact queries.

### Fixed

- Prevented hybrid adapters from linking unimported or out-of-scope names, string, rune, character, and macro contents, mismatched Go packages, and test-only Go declarations; added owned cross-file Rust implementation evidence, cached project declaration analysis, and Go package-peer reindexing.
